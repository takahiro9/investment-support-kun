# CLAUDE.md

## プロジェクト概要

investment-support-kun は、Claude Code の skill を UI レイヤー、Markdown ファイル（Vault, `data/vault/`）を一次データストアとする、投資先企業の意思決定構造理解を支援する CLI ファーストのアプリケーション。詳細は `tech_context/architecture.md`、ドメインモデルは `domain/README.md` を参照。

## ドキュメントの書き方 — 履歴を本文に残さない

`domain/`・`tech_context/`・`.claude/skills/` 配下の Markdown は、**常に「現状の最新状態」だけを説明する**。以下は書かない・見つけたら削除する。

- `Phase 4で導入`、`Phase 1の範囲外`、`導入Phase` のような実装フェーズ・ステップ番号
- `（2026-08-01決定）`、`2026-07-26時点の決定により` のような決定日付の注記
- `旧pricedIn`、`初版は〜だったが改訂後は〜` のような before/after の変更経緯の説明
- 実装済みの機能を「未実装」「範囲外」と書いたまま放置すること（実装が進んだら都度ドキュメントを現状に合わせて更新する）

理由: これらの情報はすぐに陳腐化し、ドキュメントを読むたびに「今も本当か」を確認するコストを生む。変更の経緯・理由・日付は **コミットメッセージ** に書く（`git log`/`git blame` が正の履歴）。ドキュメントは常に「今のあるべき姿」だけを記述する。

## 変更履歴は CHANGELOG.md へ

ルートの `CHANGELOG.md` に変更を追記する。

- 1エントリは一言（何をしたか）だけ。理由・経緯・背景はコミットメッセージ側に書き、CHANGELOG には書かない
- 新しい変更を先頭（`## Unreleased` 直下の先頭）に追加する
- ドキュメント本文に書きたくなった「〜から〜へ変更した」的な文言は、CHANGELOG.md への一言エントリとコミットメッセージに逃がす

## コミットメッセージ

変更の背景・理由・Before/After はコミットメッセージに書く（このリポジトリでは日本語）。ドキュメント側にはその要約すら残さず、必要なら該当コミットへのポインタとして `git log`/`git blame` を辿らせる。
